# Environment Variables
#
# This file documents all environment variables required by the project.
# Copy this file to `.env` and fill in the values for your local environment.
#
#   cp .env.example .env
#
# NEVER commit your `.env` file to version control.
# Real secrets must never appear in this file — use placeholder values only.
# For production secrets, use a dedicated secrets manager.
#
# See also: docs/12-deployment.md (Secrets and Configuration Management)
#           docs/07-security.md   (Secrets Management)

# ─────────────────────────────────────────────────────────────────────────────
# Application
# ─────────────────────────────────────────────────────────────────────────────

# The environment the application is running in.
# Accepted values: development | test | staging | production
APP_ENV=development

# The port the application server will listen on.
APP_PORT=3000

# ─────────────────────────────────────────────────────────────────────────────
# Database
# ─────────────────────────────────────────────────────────────────────────────

# Full connection string for the primary database.
# Example: postgresql://user:password@localhost:5432/mydb
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ─────────────────────────────────────────────────────────────────────────────
# Authentication
# ─────────────────────────────────────────────────────────────────────────────

# Secret used to sign and verify authentication tokens.
# Must be a long, random, unpredictable string in production.
AUTH_SECRET=change-me-to-a-long-random-secret

# Token expiration time.
# Examples: 15m, 1h, 7d
AUTH_TOKEN_EXPIRY=15m

# ─────────────────────────────────────────────────────────────────────────────
# External Services
# ─────────────────────────────────────────────────────────────────────────────

# TODO: Add variables for each external service your project depends on.
# Example:
# STRIPE_SECRET_KEY=sk_test_placeholder
# SENDGRID_API_KEY=SG.placeholder
# AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
# AWS_SECRET_ACCESS_KEY=placeholder

# ─────────────────────────────────────────────────────────────────────────────
# Observability
# ─────────────────────────────────────────────────────────────────────────────

# Minimum log level to emit.
# Accepted values: debug | info | warn | error
LOG_LEVEL=debug
